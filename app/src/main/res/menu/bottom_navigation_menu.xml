<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Community -->
    <item
        android:id="@+id/community_menu_item"
        android:enabled="true"
        android:icon="@drawable/ic_community"
        android:title="@string/community_menu_item" />
    <!-- Friends -->
    <item
        android:id="@+id/friends_menu_item"
        android:enabled="true"
        android:icon="@drawable/ic_friends"
        android:title="@string/friends_menu_item" />
    <!-- Draw -->
    <item
        android:id="@+id/draw_menu_item"
        android:enabled="true"
        android:icon="@drawable/ic_draw_path"
        android:title="@string/draw_menu_item" />
    <!-- History -->
    <item
        android:id="@+id/history_menu_item"
        android:enabled="true"
        android:icon="@drawable/ic_history"
        android:title="@string/history_menu_item" />
    <!-- Settings -->
    <item
        android:id="@+id/settings_menu_item"
        android:enabled="true"
        android:icon="@drawable/ic_settings"
        android:title="@string/settings_menu_item" />
</menu>